# 1.如果去掉前一个或者两个括号就会报错：SyntaxError: invalid syntax
#   如果去掉后一个括号就会只显示三个点：...
# 2.如果去掉前一个或者去掉两个引号就会报错：SyntaxError: invalid syntax
#   如果去掉后一个引号就会报错：SyntaxError: EOL while scanning string literal
# 3.用减号创建一个负数，在一个数字前再加上个加号输出得到的结果与不加加号是一样的。
#   2++2输出得到的值为4
# 4.python中前导0会报错：SyntaxError: leading zeros in decimal integer literals are not permitted; use an 0o prefix for octal integers
# 5.如果两个值之间没有运算符，就会按顺序连着输出两个值
